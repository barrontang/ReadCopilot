# ReadCopilot PRD v3.0

> 状态：升级版 PRD（基于当前仓库能力与外部调研）
> 平台：macOS 14+，iOS 17+
> 产品原则：本地优先、BYOK、证据可追溯、先编译知识再展示图谱、先可读后可视化

## 0. 当前仓库基线

本版 PRD 以当前仓库已实现能力为基线，不从零假设产品：

| 模块 | 当前能力 | 升级缺口 |
|---|---|---|
| 阅读主页 | 已有周/月/年/累计切换、统计卡片、时段热力图、品类图、最近阅读与书库 | 缺少“点击卡片进入细分图表”的下钻能力 |
| Copilot 分析 | 已支持单本/类别/全库范围与多模板分析、导出报告 | 缺少与“阅读笔记本”联动的编排输出 |
| 笔记导出 | 已支持单本划线与笔记 Markdown 导出、报告 PDF 导出 | 缺少按阅读顺序排版的 Notebook 模块、日历图片导出与图书式 PDF |
| 知识库 | 已支持本地持久化、搜索、来源保留、主题提取 | 主题层仍偏轻量，缺少编译队列、卡片版本、冲突检查 |
| 知识图谱 | 已有基础“分类—书籍”图形呈现 | 尚未升级为“主题—图书—证据—时间”可迭代图谱 |

结论：当前仓库已经完成阅读分析产品的基础闭环，下一阶段重点不是重做，而是补齐 Notebook、知识编译、图谱演进和主页图表下钻。

## 1. 产品升级摘要

ReadCopilot 的下一阶段目标，是把“同步—分析—导出—沉淀”闭环升级为“同步—整理—编译—图谱—出版”闭环：

1. 新增 **阅读笔记本 Notebook** 模块，把划线与想法按阅读顺序整理为可读、可导出、可出版的笔记流；
2. 把知识库从“可搜索的笔记集合”升级为“可增量编译的知识卡片系统”；
3. 把知识图谱从展示型视图升级为“主题、来源、时间均可追溯”的工作台；
4. 把阅读主页从“汇总看板”升级为“支持点击下钻的数据入口”；
5. 输出物从 Markdown/PDF 扩展为 **图片全集导出 + 图书模板 PDF**。

核心成功场景：用户同步微信读书数据后，可以在一次工作流中完成“看总览 → 点卡片看细节 → 进入图书或时间段 → 整理为笔记本 → 导出图片/PDF → 编译为知识卡片 → 在图谱中查看跨书连接”。

## 2. 调研与对标结论

### 2.1 微信读书与现有导出工具

微信读书已经擅长展示累计阅读指标；同类导出工具已经证明“原文+笔记+元数据”的可移植输出是重度用户刚需。ReadCopilot 继续保持差异化：

- 不止导出原始记录；
- 必须保留证据链；
- 导出后仍可继续编译、检索、汇总和复用。

### 2.2 Apple Books 风格输出结论

基于 Apple Books 格式与视觉调研，Notebook/PDF 输出应遵循以下结论：

- 采用书籍式强版心、留白、页眉页脚和连续页码；
- 纸张底色优先暖白，不用纯白高对比硬边；
- 字体以高可读正文和克制的标题层级为主；
- 导出的图片页必须可独立阅读，且可进一步装订为 PDF；
- 图书式 PDF 需要封面、扉页、目录、正文、页码和导出说明页；
- 风格参考 Apple Books，但不机械复刻 UI。

### 2.3 仪表盘下钻设计结论

调研表明，阅读/习惯类产品的主页图表应采用“汇总卡片作为入口、详细图表分层展开”的模式：

- 汇总卡片必须明确可点击；
- 点击后保留上下文，不让用户迷路；
- 优先下钻到趋势、分布、时间明细和书籍明细；
- 一张主页只放高价值摘要，不把所有图都堆在首屏。

### 2.4 编译式知识库与增量图谱结论

参考 LLMWiki 与增量知识图谱实践，下一阶段必须坚持：

- 只增量处理变更笔记，不全量重编译；
- 每个知识卡片都附来源、编译时间、置信度和版本；
- 图谱只消费编译结果，不直接建立在原始笔记堆上；
- 优先解决“来源、冲突、重复、过期”，再追求炫酷图形。

## 3. 用户与任务

### 3.1 目标用户

- 已拥有 `wrk-` Key 的微信读书重度用户；
- 有持续划线和写想法习惯；
- 愿意将阅读资料长期沉淀为私人知识资产；
- 希望将笔记导出为适合收藏、回顾、打印或分享的“书籍式内容”。

### 3.2 核心任务

| JTBD | 用户结果 |
|---|---|
| 当我回顾一段时间的阅读时 | 能先看汇总，再点进更细的图表与明细 |
| 当我整理某本书的划线和想法时 | 能得到按阅读顺序排列的 Notebook |
| 当我准备保存或打印阅读档案时 | 能导出带页码的图片全集与图书式 PDF |
| 当我的笔记持续增长时 | 能增量编译为主题卡片，而不是反复全量重跑 |
| 当我理解跨书关系时 | 能在图谱中追溯到图书、时间和原始证据 |

## 4. 信息架构与原型

### 4.1 一级导航

1. **阅读主页**：指标总览、图表和下钻入口；
2. **Notebook**：按阅读顺序与日历维度整理划线和想法；
3. **Copilot**：范围分析、模板生成、书评与报告；
4. **知识库**：知识卡片、编译状态、图谱与检索；
5. **设置**：数据源、模型、隐私和本地数据。

### 4.2 阅读主页低保真原型

```text
┌ 周 | 月 | 年 | 累计 ───────────────────── 同步 ┐
│ [阅读时长] [阅读天数] [完读率] [书架数]         │
│   ↑ 每个卡片都可点击进入趋势/分布/明细图表      │
├ 时段热力图 ─ 品类分布 ─ 阅读趋势 ─ 最近阅读 ───┤
│ 图表区支持切换：总览 / 时间 / 书籍 / 类别       │
└──────────────────────────────────────────────────┘
```

### 4.3 Notebook 低保真原型

```text
┌ 图书 / 时间范围 / 导出 ────────────────────────┐
│ 模式：[阅读顺序] [日历]                         │
├ 2026-09-03  Thu                                 │
│ 08:42  原文：......                              │
│       笔记：......                              │
│ 09:15  原文：......                              │
│       笔记：......                              │
├ 月历视图：                                      │
│  1  2  3● 4● 5                                  │
│ 点击某天 → 当日图片页预览 / 全量导出            │
└──────────────────────────────────────────────────┘
```

### 4.4 Copilot 低保真原型

```text
范围：[单本 | 类别 | 全部]  对象：[选择器]
模板：[阅读教练 / 批判阅读 / 行动转化 / 跨书综合 / 知乎书评]
Notebook 引用：[当前图书笔记本 | 当前时间段 | 手动选择]
────────────────────────────────────
发送范围与服务商告知 → 明确同意 → 获取笔记 → 生成
报告正文（证据引用）        [导出 MD] [导出 PDF]
附录：Notebook 摘要 / 原文 / 对应笔记
```

### 4.5 视觉规范

- 延续现有暖灰纸张底色与墨绿强调色；
- Notebook 与 PDF 的阅读体验参考 Apple Books：版心稳定、页码克制、留白充分；
- 图片页优先适配 3:4 与 A4/Letter 打印裁切安全区；
- 中文正文行高 1.6–1.8，单行长度控制在舒适阅读范围；
- 数据图表继续弱化网格线，重点突出趋势和可点击入口；
- 所有模块必须有加载、空、错误、重试和无障碍表现。

## 5. 功能需求

### F1 数据同步

- 保持真实 Key 校验；
- 支持书架、周/月/年/累计阅读数据；
- 全量笔记同步必须分页、限速、断点续传；
- 原始记录保留 `bookId`、章节、类型、原文、想法、时间和位置；
- API 字段缺失时降级，不丢弃已同步数据；
- 为 Notebook/知识编译补齐“时间”和“阅读顺序”所需字段。

**验收：** 中断同步后可继续；重复同步幂等；无 Key 时不发请求；Notebook 依赖字段缺失时有降级占位。

### F2 阅读主页与图表下钻

- 保留周、月、年、累计周期切换；
- 首页继续展示时长、天数、书架、完读率、阅读历程、日均阅读等摘要；
- 每个汇总卡片必须可点击，进入对应细维度页面或抽屉；
- 下钻维度至少包含：趋势、日历、时段、类别、书籍明细；
- 细分图表支持“从整体到单日/单书”的逐层收窄；
- 支持从图表反向进入图书详情、Notebook 或 Copilot。

**验收：** 点击卡片必有反馈；返回后保留原周期和筛选状态；无数据时展示原因与建议动作。

### F3 Notebook 模块

- 新增独立 Notebook 模块；
- 核心列表默认按阅读顺序展示，而不是按抓取顺序；
- 每条记录固定为两行主结构：一行原文、一行笔记；附带时间戳、章节/位置元数据；
- 无笔记的划线也可保留，并显示为空笔记状态；
- 支持“阅读顺序”和“日历模式”两种浏览方式；
- 日历模式按天聚合，展示每日记录数、活跃时间与图书分布；
- 支持按图书、类别、日期范围筛选；
- 支持从 Notebook 条目跳转到知识卡片编译或 Copilot 分析。

**验收：** 同一本书内顺序稳定；同一天记录分组正确；无时间字段时降级到同步时间或位置顺序。

### F4 Notebook 图片与图书式 PDF 导出

- Notebook 支持全量导出图片；
- 图片导出按页输出，**1 张图片 = 1 页**；
- 每页必须带页码、日期/图书信息和统一版式；
- 支持日历模式整月导出与阅读顺序全集导出；
- 支持把导出的页集合装订为图书模板 PDF；
- 图书模板 PDF 至少包含：封面、导出说明、目录、正文页、页码；
- 视觉渲染参考 Apple Books 的书籍阅读风格；
- 保留 UTF-8 Markdown 导出，作为开放格式备份。

**验收：** 多页图片导出页码连续；PDF 不裁字、不丢页；中英文书名和长笔记均可正确排版。

### F5 Copilot 分析与书评

- 保持单本、类别、全库分析；
- 保持阅读教练、批判阅读、行动转化、跨书综合、知乎书评模板；
- 新增对 Notebook 的引用能力：分析时可选择“当前图书笔记本”或“当前时间段笔记本”；
- 模板继续声明输出结构和证据规则；
- 发送 LLM 前显示范围、笔记条数、服务商和 Notebook 引用范围，并由用户确认；
- 报告导出时可选择附带 Notebook 附录。

**验收：** 切换分析范围不会复用旧 Notebook 上下文；每条跨书结论都能回到来源图书与原始条目。

### F6 知识编译功能优化

- 将当前轻量主题提取升级为多阶段知识编译：
  1. 原始层 `RawNote`
  2. 规范化层 `NormalizedNote`
  3. 主题/命题层 `KnowledgeCard`
  4. 关系层 `KnowledgeEdge`
- 编译仅处理新增或变更笔记；
- 每张卡片保留来源条目、代表引文、首次生成时间、最近编译时间、版本、置信度；
- 引入编译队列、失败重试和局部重编译；
- 增加 lint：无来源、重复主题、冲突命题、长期未更新卡片；
- 查询优先返回卡片，再下钻到原文和 Notebook 条目。

**验收：** 重复编译不产生重复卡片；新增少量笔记时不触发全量重编译；任意卡片都可追溯来源。

### F7 知识图谱迭代优化

- 图谱从当前“分类—书籍”视图升级为“主题—图书—证据—时间”视图；
- 第一阶段展示稳定的主题节点与来源图书关系；
- 第二阶段加入代表证据、主题聚类、时间演进与冲突提示；
- 节点点击后进入卡片详情，而不是只显示静态名称；
- 允许按图书、类别、时间范围、主题热度过滤；
- 图谱必须消费知识编译结果，不直接读取未整理原始数据；
- 保持图谱轻量可读，避免为动画牺牲证据可追溯性。

**验收：** 任意节点 2 步内可回到原始笔记；图谱筛选后节点/边统计一致；空图谱时明确说明原因。

### F8 导出与互操作

- 保留单本及分析结果 Markdown/PDF 导出；
- 为 Notebook、分析报告、知识卡片建立统一导出索引；
- 输出目录包含 `index.json`、封面信息、导出时间、页数与来源图书清单；
- 后续兼容 Obsidian、Bear 或其他知识管理工具导入。

## 6. 数据与架构

```text
SwiftUI Presentation
  ├─ Reading Home
  ├─ Notebook Workspace
  ├─ Copilot Workspace
  ├─ Knowledge Base / Graph
  └─ Settings
Domain
  ├─ SyncCoordinator
  ├─ ReadingAnalytics
  ├─ NotebookComposer
  ├─ AnalysisOrchestrator
  ├─ KnowledgeCompiler
  ├─ GraphProjector
  └─ ExportService
Infrastructure
  ├─ WeReadGateway
  ├─ OpenAICompatibleClient
  ├─ SwiftData repositories
  ├─ Keychain
  ├─ Image renderer
  └─ PDF book renderer
```

建议新增/升级实体：

- `BookRecord`：书籍元数据和同步状态；
- `RawNote`：原文、想法、章节、位置、时间；
- `NotebookEntry`：排序后的显示单元；
- `NotebookPage`：图片页、页码、模板和导出信息；
- `ReadingSnapshot`：周期、起止时间和统计值；
- `AnalysisReport`：范围、模板、模型、证据引用和版本；
- `KnowledgeCard`：主题、命题、摘要、来源、版本、置信度；
- `KnowledgeEdge`：主题关系、跨书关系、冲突或引用关系；
- `CompileCheckpoint`：编译游标、失败原因和重试状态。

约束：

- 网络 DTO、领域模型和 SwiftData 实体分离；
- 排序逻辑、编译逻辑、导出渲染逻辑不得写入 View；
- 图片渲染与 PDF 装订拆分，便于单测和复用。

## 7. 隐私、安全与可靠性

- WeRead 与 LLM Key 仅存 Keychain；
- LLM Base URL 默认仅允许 HTTPS；本机 Ollama 可显式允许 HTTP；
- 请求错误不得回显 Key 或完整服务端响应；
- 私密笔记默认排除；分析必须逐次确认；
- 无遥测、无自有后端、无自动发布；
- 导出使用系统文件权限；
- 排序、分页、去重、编译、图谱投影和图片/PDF 导出均需单元测试；
- 网络通过 `URLProtocol` mock，端到端测试不得依赖真实用户 Key。

## 8. 质量指标

| 指标 | 目标 |
|---|---|
| 首次同步成功率 | ≥ 95%（有效 Key、网络正常） |
| 单本分析完成率 | ≥ 95% |
| Notebook 导出成功率 | ≥ 99% |
| 图片页码连续正确率 | 100% |
| 有证据结论占比 | 100% 或明确标注“推断” |
| 增量编译重复卡片 | 0 |
| 图谱来源可追溯率 | 100% |
| 核心领域代码单测覆盖 | ≥ 80% |

## 9. 迭代计划

### M1：Notebook MVP

- 新增 Notebook 一级导航；
- 打通单本笔记按阅读顺序展示；
- 支持时间戳、两行结构、日期分组；
- 完成阅读主页卡片点击下钻的第一批图表。

### M2：图片与图书式导出

- Notebook 图片全集导出；
- 1 图 1 页、连续页码；
- 图书模板 PDF、封面、目录和正文装订；
- 分析报告与 Notebook 附录联动。

### M3：知识编译升级

- 从主题标签升级为 `KnowledgeCard`；
- 增量编译、版本、重试和 lint；
- 查询与 Notebook 反向链接。

### M4：知识图谱工作台

- 主题—图书—证据图谱；
- 时间演进、冲突提示、过滤与聚类；
- 图谱详情与卡片详情闭环。

### M5：高级洞察与互操作

- 主页更多维图表；
- 导出索引与外部知识库互操作；
- 更细粒度阅读策略建议与长期回顾模板。

## 10. 发布门禁

每个里程碑必须完成：需求验收用例、单元测试、错误态检查、隐私检查、Secret Scan、静态安全扫描、macOS/iOS 构建和回归测试。未经真实 API 响应样本验证的字段不得标记为完成。

## 11. 已知风险

| 风险 | 对策 |
|---|---|
| Skill API 无公开 SLA、字段变化 | DTO 容错、版本升级提示、契约样本测试 |
| 时间字段不完整，影响 Notebook 排序 | 建立多级排序回退：显式时间 → 阅读位置 → 同步时间 |
| 图书式 PDF 复杂排版带来性能问题 | 图片页和 PDF 装订分层，先缓存页再合成 |
| 全库分析成本和上下文超限 | 范围预估、截断提示、分批综合、知识卡片缓存 |
| 模型幻觉 | 强制证据 ID、分离事实/推断、导出附来源 |
| 图谱过度炫技 | 先保证来源可追溯、可过滤、可解释，再增加视觉增强 |

## 12. 更新建议与设计 guideline

### 12.1 产品策略建议

- 先做 **Notebook MVP**，再做重型 PDF 装订；
- 先做 **知识卡片**，再做复杂图谱；
- 主页下钻优先服务“行动入口”，不是为了展示更多图；
- 所有 AI 输出都要继续围绕“可回到原文证据”设计。

### 12.2 交互 guideline

- 卡片、图谱节点、Notebook 条目都要可点击且有反馈；
- 任意深层页面都要保留“返回上层上下文”的路径；
- 一次只突出一个主任务：看总览、整理笔记、做分析、看图谱；
- 不让用户在首页同时面对过多图表和过多操作。

### 12.3 内容 guideline

- 原文优先、解释其次、结论最后；
- 笔记本内容强调“回看”和“收藏”，不是“即时聊天”；
- 导出文件既要适合机器再利用，也要适合人直接阅读；
- 所有模板文案都要减少术语堆叠，突出可执行结论。

### 12.4 工程 guideline

- 所有排序、编译、导出都要可测试；
- 所有增量流程都要幂等；
- 所有跨模块跳转都要带明确上下文对象；
- 视图层只负责呈现，不承担解析、编译和导出逻辑。

## 13. 外部参考

- Apple Books Formatting and Content Guidelines
- Apple Books Asset Guide
- Apple Design Resources
- Dashboard progressive disclosure / drill-down design best practices
- LLMWiki / Incremental Knowledge Graph Construction related practices
