# ReadCopilot PRD v2.0

> 状态：实施基线
> 平台：macOS 14+，iOS 17+
> 产品原则：本地优先、BYOK、证据可追溯、先编译知识再展示图谱

## 1. 产品摘要

ReadCopilot 是微信读书用户的个人阅读分析与知识编译工具。它不替代阅读器，而是把书架、阅读行为、划线和想法转换为：

1. 可解释的阅读行为洞察；
2. 有原文证据的 AI 笔记分析；
3. 可继续编辑和发布的书评；
4. 可导出的 Markdown/PDF 阅读档案；
5. 可积累、可检索、可关联的个人知识库。

核心成功场景：用户同步微信读书数据后，在 3 分钟内选定分析范围和模板，得到一份每条结论均可追溯至原文或个人笔记的报告。

## 2. 背景与对标结论

### 2.1 微信读书

微信读书「我的阅读」已擅长展示累计时长、阅读天数、品类偏好、时段偏好和年度总结。ReadCopilot 不复制社交排名或分享海报，而是在相同数据上增加私人教练式解释、周期切换和行动建议。

可用 Skill API（v1.0.4）：

| 能力 | API |
|---|---|
| 周/月/年/累计阅读数据 | `/readdata/detail` |
| 书架 | `/shelf/sync` |
| 笔记本概览 | `/user/notebooks` |
| 划线、想法 | `/book/bookmarklist`、`/review/list/mine` |
| 图书与章节 | `/book/info`、`/book/chapterinfo` |
| 搜索与推荐 | `/store/search`、`/book/recommend`、`/book/similar` |

### 2.2 同类开源产品

WeReadNotesExporter、weread-cli、微信读书 Obsidian 同步插件证明：逐书 Markdown、原文与笔记配对、YAML 元数据和可移植文件是重度用户的基础需求。ReadCopilot 的差异化不在“导出”，而在“分析后仍保留证据链”。

### 2.3 编译式知识库

参考 Andrej Karpathy 的 LLMWiki 思路：

- `raw`：不可变的原始划线、想法和图书元数据；
- `wiki`：由模型增量维护的主题卡片、摘要、分歧和交叉引用；
- `ingest/query/lint`：增量编译、查询、矛盾与过期检查；
- graph 仅是 `wiki` 的呈现方式，不是知识库地基。

因此必须先完成本地笔记持久化、主题卡片和来源引用，再完善图谱交互。

## 3. 用户与任务

### 3.1 目标用户

- 已拥有 `wrk-` Key 的微信读书重度用户；
- 有持续划线或写想法的习惯；
- 愿意使用自己的 OpenAI 兼容 LLM Key；
- 希望形成书评、文章选题或长期知识资产。

### 3.2 核心任务

| JTBD | 用户结果 |
|---|---|
| 当我回顾一段时间的阅读时 | 知道投入、偏好、完读情况和下一步调整 |
| 当我读完一本或一类书时 | 用模板分析笔记，结论可追溯到证据 |
| 当我需要迁移资料时 | 导出包含原文与对应笔记的 MD/PDF |
| 当我准备写书评时 | 生成不捏造事实、可编辑的知乎 Markdown 初稿 |
| 当笔记不断增加时 | 增量编译为主题卡片并查看跨书关联 |

## 4. 信息架构与原型

### 4.1 一级导航

1. **阅读主页**：仪表盘与书库在同一页面；
2. **Copilot**：范围、模板、授权确认、报告和导出；
3. **知识库**：主题卡片、来源和图谱；
4. **设置**：数据源、模型、隐私和本地数据。

### 4.2 阅读主页低保真原型

```text
┌ 周 | 月 | 年 | 累计 ───────────────────── 同步 ┐
│ 阅读时长  阅读天数  完读率  书架数               │
│ 时段热力图 / 品类分布 / 教练一句话               │
├ 我的书库 ───────── 搜索 ───────── 状态筛选 ─────┤
│ [封面 书名 作者 标签] [封面 书名 作者 标签] ... │
└──────────────────────────────────────────────────┘
```

点击任意图书进入 Copilot，并自动选中该书。主页不再设置独立“书库”导航。

### 4.3 Copilot 低保真原型

```text
范围：[单本 | 类别 | 全部]  对象：[选择器]
模板：[阅读教练 / 批判阅读 / 行动转化 / 跨书综合 / 知乎书评]
模板说明
────────────────────────────────────
发送范围与服务商告知 → 明确同意 → 获取笔记 → 生成
报告正文（证据引用）        [导出 MD] [导出 PDF]
附录：图书原文 → 对应笔记
```

### 4.4 视觉规范

- 原生 macOS 信息密度，参考 Things 3、Reeder、Bear；
- 暖灰纸张背景、墨绿单一强调色；
- 中文正文行高 1.6–1.8，长报告最大阅读宽度；
- 图表弱化网格线，不使用无含义装饰色；
- 所有状态必须有加载、空、错误和重试表现；
- 键盘导航、VoiceOver 标签和动态字号属于验收范围。

## 5. 功能需求

### F1 数据同步

- Key 验证必须调用真实接口；
- 支持书架、周/月/年/累计阅读数据；
- 全量笔记同步必须分页、限速、断点续传；
- 原始记录保留 `bookId`、章节、类型、原文、想法、时间和位置；
- API 字段缺失时降级，不丢弃已同步数据。

**验收：** 中断同步后可继续；重复同步幂等；无 Key 时不发请求。

### F2 阅读主页

- 仪表盘与书库合并；
- 周、月、年、累计周期切换；
- 展示时长、天数、书架、读完数、完读率、偏好时段和品类；
- 搜索书名/作者，筛选在读/读完；
- 点击书籍直接进入单本分析。

**验收：** 周期切换后标题和数据口径一致；空书架仍可进入设置。

### F3 Copilot 分析

- 范围：单本、类别、全部；
- 缺省模板：阅读教练；
- 模板：批判性阅读、行动转化、跨书综合、知乎书评；
- 模板必须声明输出结构和证据规则；
- 发送 LLM 前显示范围、笔记数量和服务商，并由用户确认；
- 类别/整体分析标注每条结论的来源图书；
- 控制输入数量并提示截断，不静默丢弃。

**验收：** 无证据的结论标记为推断；切换范围不会复用旧报告；失败可重试。

### F4 导出

- 单本及分析结果支持 Markdown 与 PDF；
- 正文包含图书元数据、分析报告、原文和对应笔记；
- Markdown 使用 UTF-8，可加入 Obsidian 兼容 YAML frontmatter；
- 用户通过系统文件选择器决定位置；
- 文件名安全、可排序，不写死桌面目录；
- 后续提供报告目录与 `index.json` 可移植索引。

**验收：** 中英文书名均可导出；PDF 多页不截断；取消导出不报错。

### F5 阅读时长分析

- 第一阶段使用 Skill 提供的周/月/年/累计口径；
- 第二阶段持久化周期快照，支持用户指定起止日期；
- 输出趋势、峰值、稳定性和个人基线，不做社交排名；
- 建议借鉴运动应用的“负荷—恢复—连续性”叙事，但不得把阅读时长等同于阅读质量。

### F6 书评生成

- 输入仅使用所选书的元数据、原文和笔记；
- 输出知乎适用 Markdown：标题、导语、核心观点、个人分析、保留意见、适合读者、结语；
- 不虚构情节、引语、个人经历或作者立场；
- 生成结果始终为草稿，用户编辑后自行发布。

### F7 编译式知识库

- `RawNote` 为只增不改的来源层；
- `KnowledgeConcept` 包含主题、摘要、来源、代表引文、相关主题、更新时间和置信度；
- ingest 仅重编译受新笔记影响的卡片；
- query 优先查询卡片，再回溯原始证据；
- lint 检查无来源结论、矛盾、重复主题和过期卡片；
- graph 展示“主题—图书—证据”关系，节点必须能回到来源。

## 6. 数据与架构

```text
SwiftUI Presentation
  ├─ Reading Home
  ├─ Copilot Workspace
  ├─ Knowledge Base
  └─ Settings
Domain
  ├─ SyncCoordinator
  ├─ ReadingAnalytics
  ├─ AnalysisOrchestrator
  ├─ KnowledgeCompiler
  └─ ExportService
Infrastructure
  ├─ WeReadGateway
  ├─ OpenAICompatibleClient
  ├─ SwiftData repositories
  ├─ Keychain
  └─ FileDocument/PDF renderer
```

持久化实体：

- `BookRecord`：书籍元数据和同步状态；
- `RawNote`：原文、想法、章节、位置、时间；
- `ReadingSnapshot`：周期、起止时间和统计值；
- `AnalysisReport`：范围、模板、模型、证据引用和版本；
- `KnowledgeConcept`：编译内容与来源关系；
- `SyncCheckpoint`：游标、失败原因和重试状态。

网络 DTO、领域模型和 SwiftData 实体分离，禁止在 View 中解析 JSON。

## 7. 隐私、安全与可靠性

- WeRead 与 LLM Key 仅存 Keychain；
- LLM Base URL 默认仅允许 HTTPS；本机 Ollama 可显式允许 HTTP；
- 请求错误不得回显 Key 或完整服务端响应；
- 私密笔记默认排除；分析必须逐次确认；
- 无遥测、无自有后端、无自动发布；
- 导出使用系统文件权限；
- 解析、分页、去重、Prompt 和导出均需单元测试；
- 网络通过 `URLProtocol` mock，端到端测试不得依赖真实用户 Key。

## 8. 质量指标

| 指标 | 目标 |
|---|---|
| 首次同步成功率 | ≥ 95%（有效 Key、网络正常） |
| 单本分析完成率 | ≥ 95% |
| 有证据结论占比 | 100% 或明确标注“推断” |
| 导出成功率 | ≥ 99% |
| 增量同步重复记录 | 0 |
| 核心领域代码单测覆盖 | ≥ 80% |

产品指标全部本地计算且默认不上传：首次分析完成、报告保存/导出、知识卡片回访。

## 9. 迭代计划

### M1：可用闭环

- 阅读主页（仪表盘+书库）；
- 四周期读取；
- 单本笔记分析、书评模板；
- 原文与笔记 Markdown/PDF 导出；
- 基础测试。

### M2：可靠数据层

- SwiftData、全量笔记分页同步、断点续传；
- 类别/整体分析；
- 自定义时间区间和报告历史；
- 导出索引与 Obsidian 互操作。

### M3：知识编译

- 主题卡片、增量 ingest、全文检索；
- 跨书综合、来源追溯、知识 lint；
- 查询时的 token 预算与缓存。

### M4：知识图谱

- 主题—图书—证据交互图谱；
- 聚类、过滤、时间演进和矛盾提示；
- iOS 适配与无障碍完善。

## 10. 发布门禁

每个里程碑必须完成：需求验收用例、单元测试、错误态检查、隐私检查、Secret Scan、静态安全扫描、macOS/iOS 构建和回归测试。未经真实 API 响应样本验证的字段不得标记为完成。

## 11. 已知风险

| 风险 | 对策 |
|---|---|
| Skill API 无公开 SLA、字段变化 | DTO 容错、版本升级提示、契约样本测试 |
| 全库分析成本和上下文超限 | 范围预估、截断提示、分批综合、知识卡片缓存 |
| 模型幻觉 | 强制证据 ID、分离事实/推断、导出附来源 |
| 用户笔记隐私 | 本地默认、逐次同意、私密排除、BYOK |
| 图谱华而不实 | 先构建可查询卡片和来源关系，再渲染 graph |
